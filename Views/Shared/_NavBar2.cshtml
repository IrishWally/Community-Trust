<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <div class="col-lg-12"><img src="~/Content/Images/CT_logo40.jpg" class="img-rounded" alt="Community Trust" height="150" /> </div>
        </div>
        <ul class="nav navbar-nav collapse navbar-collapse show">
            <li class="active"><button type="button">@Html.ActionLink("Home", "Index", "Home")</button></li>
            <li style="margin-left:5%"><button type="button">@Html.ActionLink("About", "About", "Home")</button></li>
            <li style="margin-left:5%"><button type="button">@Html.ActionLink("Contact", "Contact", "Home")</button></li>
            <li style="margin-left:5%"><button type="button">@Html.ActionLink("Make a Submission", "NewCause", "Submission")</button></li>
            <li style="margin-left:5%"><button type="button">@Html.ActionLink("Voting", "ShowAllSubmissions", "Submission")</button></li>
            <li style="margin-left:5%"><button type="button">@Html.ActionLink("Contributions", "Index", "Contributions")</button></li>
            <li style="margin-left:5%"><button type="button">@Html.ActionLink("Log In", "Login", "Account")</button></li>

            <!--<li class="dropdown" style="margin-left:5%">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            Sign In
            <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
            <li>
                 using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new {  s class = "navbar-form navbar-right form-inline", role = "form" }))
                {
                     Html.AntiForgeryToken()
                     Html.ValidationSummary(true, "", new {  class = "text-danger" })
                    <div class="form-group">
                        Html.LabelFor(m => m.Email, new {  class = "col-md-2 control-label sr-only" })

                        Html.TextBoxFor(m => m.Email, new { class = "form-control", id = "Email", placeholder = "Email", autofocus = "true", required = "true" })
                         Html.ValidationMessageFor(m => m.Email, "", new { class = "text-danger sr-only" })

                    </div>
                    <div class="form-group">
                         Html.LabelFor(m => m.Password, new {  class = "col-md-2 control-label sr-only" })

                        Html.PasswordFor(m => m.Password, new {  class = "form-control", id = "Password", placeholder = "Password" })
                         Html.ValidationMessageFor(m => m.Password, "", new {  class = "text-danger sr-only" })
                    </div>

                    <input type="submit" value="Sign in" class="btn btn-success" />

                }

            </li>
        </ul>
    </li>-->
        </ul>
    </div>
</nav>

